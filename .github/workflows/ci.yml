name: Android build

on: push

jobs:
  build:
    name: what is this really
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: Set up JDK
        uses: actions/setup-java@v1
        with:
          java-version: 1.8

      - name: Run test
        run: ./gradlew test

      - name: Build Proejct
        run: ./gradlew assemble

      - name:
        uses: yutailang0119/action-android-lint@v1.1.0

